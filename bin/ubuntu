#!/usr/bin/env bash
#
# Shell into the Ubuntu target container, or run a command in it.
#
#   bin/ubuntu                    interactive shell
#   bin/ubuntu puppet agent -t    apply the catalog

set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/lib/classroom.sh"

enter agent-ubuntu "$@"
